import * as React from "react";import Container from "../../atoms/Container";interface LandingTemplateProps {    header: JSX.Element,    footer: JSX.Element,    headline?: JSX.Element,    projects?: JSX.Element,    seoSlider?: JSX.Element,    subscribe?: JSX.Element,    reviews?: JSX.Element,    contactUs?: JSX.Element,    staff?: JSX.Element,    priceList?: JSX.Element,    guarantees?: JSX.Element}const Index: React.FC<LandingTemplateProps> = (    {        header,        footer,        children,        headline,        projects,        seoSlider,        subscribe,        reviews,        contactUs,        staff,        priceList,        guarantees    }) => {    return (        <React.Fragment>            {header}            <main className={'main'}>                {headline}                <Container className={'container_desktop'}>                    {projects}                </Container>                {seoSlider}                <Container className={'container_desktop'}>                    {staff}                    {guarantees}                    {priceList}                    {children}                </Container>                {subscribe}                <Container className={'container_desktop'}>                    {reviews}                    {contactUs}                </Container>            </main>            {footer}        </React.Fragment>    )};export default Index